import { Utils, WalletClient, PushDrop, GlobalKVStore } from "@bsv/sdk";

const wallet = new WalletClient("auto", "localhost");
const pushdrop = new PushDrop(wallet);

export async function uploadMessage() {
  
}
